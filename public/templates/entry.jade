extends layout

block prepend title
  | #{ page.title } | 

block description
  meta(name='description', content=page.metadata.abstract)

block content
  article
    header
      h1=page.title
    section.content!= page.html
    small
      p.muted
        include ../includes/date
